{
  "name": "open-road-prototype",
  "version": "2.0.0",
  "description": "Mobile-first 3D open-road racing prototype PWA",
  "private": true,
  "scripts": {
    "test": "node --check src/main.js && node --check src/core/app.js && node --check service-worker.js"
  }
}
